import React from 'react'
import EditProfile from './EditProfile'
import MyCourses from './MyCourses'

// ** This page will essentially act as the dashboard for a signed in user, with their profile info and course collections displayed
// ** Might just make a Dashboard components though, so I can toggle betweenProfile and EditProfile components

const Profile = () => {
  return (
    <div>Profile</div>
  )
}

export default Profile